#include <stdio.h>
#include "file_move.h"

void move_files(const char* source_dir, const char* target_dir) {
    printf("Déplacement des fichiers de '%s' vers '%s' (logique à implémenter)\n", source_dir, target_dir);
}
